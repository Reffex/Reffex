#include <stdio.h>
#include <stdlib.h>

int main(){
	int a;		//a é um inteiro
	int *aPtr;		//aPtr é um ponteiro para um inteiro
	
	a = 7;
	aPtr=&a;
	
	printf("\n0 endereco de a eh: %p\n", &a);
	printf("O valor de aPtr eh: %\n\n", aPtr);
	
	printf("O valor de a eh: %d\n", a);
	printf("O valor d *aPtr eh: %d\n\n", *aPtr);
	
	printf("Sabendo que *| e & complementam-se mutuamente");
	printf(".\n&*aPtr= %p\n*&aPtr= %p\n", &aPtr, *&aPtr);
	
	return 0;
}
