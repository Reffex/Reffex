#include <stdio.h>

float fatorial(int num);

int main(){
    int n = 5;
    printf("%d! = %.2f\n", n, fatorial(n));

    return 1;
}

float fatorial(int num){
    if (num == 0)
        return 1;
    else
        return num * fatorial(num-1);
}
